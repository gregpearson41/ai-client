# ─────────────────────────────────────────────────────────────
# SonarQube – AI Client project
#
# Run from the project root:
#   sonar-scanner -Dsonar.token=YOUR_TOKEN
#
# The token is intentionally omitted here so it is never
# committed to the repo.  Pass it on the command line or set
# the SONAR_TOKEN env var before scanning.
# ─────────────────────────────────────────────────────────────

# ── connection ───────────────────────────────────────────────
sonar.host.url=http://172.24.1.159:9000/sonar/
sonar.token=squ_72ed221afc21637bd734630d9ebd686cd8ac8364

# ── top-level project ────────────────────────────────────────
sonar.organization=AI-Tool
sonar.projectKey=ai-tool
sonar.projectName=ai-tool
sonar.modules=admin-backend,admin-dashboard,client-frontend

# ── global exclusions (apply to every module) ───────────────
sonar.exclusions=**/node_modules/**,**/build/**

# ── admin-backend (Express / CommonJS) ──────────────────────
admin-backend.sonar.projectName=Admin Backend
admin-backend.sonar.sources=src

# ── admin-dashboard (React / TypeScript) ────────────────────
admin-dashboard.sonar.projectName=Admin Dashboard
admin-dashboard.sonar.sources=src
admin-dashboard.sonar.tests=src/**/*.test.ts,src/**/*.test.tsx
admin-dashboard.sonar.javascript.lcov.reportPaths=coverage/lcov.info

# ── client-frontend (React / TypeScript) ────────────────────
client-frontend.sonar.projectName=Client Frontend
client-frontend.sonar.sources=src
client-frontend.sonar.tests=src/**/*.test.ts,src/**/*.test.tsx
client-frontend.sonar.javascript.lcov.reportPaths=coverage/lcov.info
