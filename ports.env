# ─────────────────────────────────────────────────────────────
# AI Client – service port configuration
#
# Edit the values below to change which ports the three
# services bind to when you run  ./start.sh
#
# CLI flags passed to start.sh override these values:
#   ./start.sh --backend 6000 --dashboard 6001 --client 6002
# ─────────────────────────────────────────────────────────────

# Express API backend  (nodemon dev server)
BACKEND_PORT=5050

# Admin dashboard      (React dev server)
DASHBOARD_PORT=3000

# Client frontend      (React dev server)
CLIENT_PORT=3001
